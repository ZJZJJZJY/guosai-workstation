# addons — 资源扩充层

本目录只做"补缺口 + 引用"，不复制底层。
1. **数模内容资源**：`topic_index.md`（真题→题型→解法→可参考素材）；更多算法细节回 `<mmd>/assets/01..07` 与 `<mm>/references/model_catalog.md`；获奖点/反模式回 `<playbook>`。
2. **论文/绘图/LaTeX**：`latex_quickstart.md`；模板 `<mmd>/references/论文模板.docx` + `<mm>/templates/latex/<comp>/`；画图 `../styles/plot_dispatch.md`；期刊级绘图 `nature-figure`。
3. **代码/计算/ML**：`env_setup.md`；代码起点 `<mm>/templates/shared/code_starter/{optimization,prediction,evaluation,classification,simulation}.py`；绘图工具 matplotlib/seaborn。
4. **编排/评审类**：`../multi_ai/`、`../diagnostics/`；评分 `<mm>/scripts/score_artifact.py`；rubrics `<mm>/references/rubrics.md`。